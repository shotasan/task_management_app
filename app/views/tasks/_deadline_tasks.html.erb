<table class="table table-borderd">
  <thead>
  <tr>
    <th><%= t('tasks.title') %></th>
    <th><%= t('tasks.content') %></th>
    <th><%= t('tasks.limit_date') %></th>
    <th><%= t('tasks.status') %></th>
    <th><%= t('tasks.priority') %></th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody class="table-body">
  <% deadline_tasks.each do |task| %>
    <% if task.limit_date < Date.today %>
      <tr class="over_deadline_tasks">
        <td class="task-title"><%= task.title %></td>
        <td><%= task.content %></td>
        <td><%= task.limit_date %></td>
        <td><%= task.status %></td>
        <td><%= task.priority %></td>
        <td><%= link_to t('tasks.show'), task %></td>
        <td><%= link_to t('tasks.edit'), edit_task_path(task) %></td>
        <td><%= link_to t('tasks.delete'), task, method: :delete, data: { confirm: t('tasks.delete_confirm') } %></td>
      </tr>
    <% else %>
      <tr class="still_deadline_tasks">
        <td class="task-title"><%= task.title %></td>
        <td><%= task.content %></td>
        <td><%= task.limit_date %></td>
        <td><%= task.status %></td>
        <td><%= task.priority %></td>
        <td><%= link_to t('tasks.show'), task %></td>
        <td><%= link_to t('tasks.edit'), edit_task_path(task) %></td>
        <td><%= link_to t('tasks.delete'), task, method: :delete, data: { confirm: t('tasks.delete_confirm') } %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>